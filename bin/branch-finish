#!/bin/zsh

branchname=$1

if [ $branchname ]; then
  git checkout $branchname && git pull
  git rebase master
  git checkout master
  git merge $branchname
  git branch -d $branchname
else
  echo 'You need a branch name to finish it from'
fi
